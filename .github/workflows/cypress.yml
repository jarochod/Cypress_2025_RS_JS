name: Cypress E2E Tests

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  cypress:
    runs-on: ubuntu-latest
    
    # 1. Użycie gotowego obrazu Cypress
    # Wybieramy obraz z Node i Cypress, który będzie środowiskiem dla joba.
    container:
      # Użyjemy obrazu od Cypress, który jest optymalny do uruchamiania testów E2E
      image: cypress/included:14.5.4
      env:
        # Przekazywanie zmiennych środowiskowych (w tym Secret) bezpośrednio do kontenera
        BASE_URL: http://localhost:2221/
        ADMIN_USERNAME: admin
        ADMIN_PASSWORD: ${{ secrets.ADMIN_PASSWORD }}
    
    steps:
    - name: ⬇️ Checkout code
      uses: actions/checkout@v4
      
    - name: 🛠️ Setup and Run Cypress Tests
      # Wszystkie polecenia wykonują się wewnątrz kontenera z obrazu cypress/included:14.5.4
      run: |
        echo "Install dependencies from lockfile"
        # Instalacja zależności NPM
        npm ci
        
        echo "Start the shopping store application"
        # 1. Nadanie uprawnień i uruchomienie aplikacji w tle (operator &)
        chmod +x ./shopping-store/shopping-store-linux-amd64
        ./shopping-store/shopping-store-linux-amd64 &
        
        # 2. Daj aplikacji czas na uruchomienie (krytyczne dla stabilności)
        echo "Waiting 3 seconds for app to start..."
        sleep 3
        
        echo "Run Cypress tests"
        # 3. Uruchomienie testów Cypress
        npm run test:ci

